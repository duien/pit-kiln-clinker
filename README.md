# Pit Kiln Clinker

Firing blue clay bricks in a pit kiln makes clinker bricks

## Packaging

Make sure the version in `modinfo.json` has been updated. Run `./release` to create the zip the mod and upload it as a new release in Github.
